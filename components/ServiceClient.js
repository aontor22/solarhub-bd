"use client";
import{useState}from"react";
export default function ServiceClient(){const[msg,setMsg]=useState("");async function submit(e){e.preventDefault();const body=Object.fromEntries(new FormData(e.currentTarget).entries());const r=await fetch("/api/service-requests",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(body)});const d=await r.json();setMsg(r.ok?"Request received. Reference: "+d.request.id:(d.error||"Could not submit request."));if(r.ok)e.currentTarget.reset()}return <main className="shell section serviceGrid"><div><span className="eyebrow">INSTALLATION & SERVICE</span><h1>Tell us about your solar project.</h1><p>Capture project type, location and preferred date. Requests are stored in the database for admin follow-up.</p></div><form className="formCard" onSubmit={submit}><input required name="name" placeholder="Full name"/><input required name="phone" placeholder="Phone number"/><select required name="projectType" defaultValue=""><option value="" disabled>Project type</option><option>Home solar</option><option>Office / commercial</option><option>Solar lighting</option><option>Battery / inverter upgrade</option><option>Maintenance / inspection</option></select><input name="location" placeholder="District / area"/><input type="date" name="preferredAt"/><textarea rows="4" name="notes" placeholder="Project details"/><button className="primaryBtn">Send request</button><p className="formMsg">{msg}</p></form></main>}
