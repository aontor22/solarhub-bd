"use client";
import{useState}from"react";
export default function ServiceClient(){const[msg,setMsg]=useState(""),[busy,setBusy]=useState(false);async function submit(e){e.preventDefault();setBusy(true);setMsg("");try{const body=Object.fromEntries(new FormData(e.currentTarget).entries());const r=await fetch("/api/service-requests",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(body)});const d=await r.json();setMsg(r.ok?"Request received. Reference: "+d.request.id:(d.error||"Could not submit request."));if(r.ok)e.currentTarget.reset()}catch{setMsg("Network error. Please try again.")}finally{setBusy(false)}}return <main className="shell section serviceGrid"><div><span className="eyebrow">INSTALLATION & SERVICE</span><h1>Tell us about your solar project.</h1><p>Share your project type, location and preferred date. Your request is stored for follow-up by the service team.</p></div><form className="formCard" onSubmit={submit}><input required maxLength="100" name="name" placeholder="Full name"/><input required maxLength="30" name="phone" placeholder="Phone number"/><select required name="projectType" defaultValue=""><option value="" disabled>Project type</option><option>Home solar</option><option>Office / commercial</option><option>Solar lighting</option><option>Battery / inverter upgrade</option><option>Maintenance / inspection</option></select><input maxLength="150" name="location" placeholder="District / area"/><input type="date" name="preferredAt"/><textarea rows="4" maxLength="1500" name="notes" placeholder="Project details"/><button disabled={busy} className="primaryBtn">{busy?"Sending…":"Send request"}</button><p className="formMsg" role="status">{msg}</p></form></main>}
