"use client";
import {useState} from "react";
export default function AuthForm({mode}){const[msg,setMsg]=useState(""),reg=mode==="register";async function submit(e){e.preventDefault();setMsg("");const body=Object.fromEntries(new FormData(e.currentTarget).entries());const r=await fetch(`/api/auth/${mode}`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(body)});const d=await r.json();if(r.ok)window.location.href="/account";else setMsg(d.error||"Request failed")}return <main className="authWrap"><form className="formCard authCard" onSubmit={submit}><span className="eyebrow">{reg?"NEW CUSTOMER":"WELCOME BACK"}</span><h1>{reg?"Create your account":"Sign in"}</h1>{reg&&<input required name="name" placeholder="Full name"/>}{reg&&<input name="phone" placeholder="Phone number"/>}<input required type="email" name="email" placeholder="Email address"/><input required minLength="8" type="password" name="password" placeholder="Password"/><button className="primaryBtn full">{reg?"Create account":"Sign in"}</button><p className="formMsg">{msg}</p><p className="muted">{reg?"Already registered?":"New here?"} <a href={reg?"/login":"/register"}>{reg?"Sign in":"Create account"}</a></p></form></main>}
