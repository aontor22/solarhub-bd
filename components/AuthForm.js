"use client";
import {useState} from "react";
export default function AuthForm({mode}){
  const[msg,setMsg]=useState(""),[busy,setBusy]=useState(false),reg=mode==="register";
  async function submit(e){e.preventDefault();setMsg("");setBusy(true);try{const body=Object.fromEntries(new FormData(e.currentTarget).entries());const r=await fetch(`/api/auth/${mode}`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(body)});const d=await r.json();if(r.ok)window.location.href="/account";else setMsg(d.error||"Request failed")}catch{setMsg("Network error. Please try again.")}finally{setBusy(false)}}
  return <main className="authWrap"><form className="formCard authCard" onSubmit={submit}><span className="eyebrow">{reg?"NEW CUSTOMER":"WELCOME BACK"}</span><h1>{reg?"Create your account":"Sign in"}</h1>{reg&&<input required maxLength="100" autoComplete="name" name="name" placeholder="Full name"/>}{reg&&<input maxLength="30" autoComplete="tel" name="phone" placeholder="Phone number"/>}<input required maxLength="254" autoComplete="email" type="email" name="email" placeholder="Email address"/><input required minLength={reg?10:1} maxLength="128" autoComplete={reg?"new-password":"current-password"} type="password" name="password" placeholder={reg?"Password (10+ chars, letter + number)":"Password"}/><button className="primaryBtn full" disabled={busy}>{busy?"Please wait…":reg?"Create account":"Sign in"}</button><p className="formMsg" role="status">{msg}</p><p className="muted">{reg?"Already registered?":"New here?"} <a href={reg?"/login":"/register"}>{reg?"Sign in":"Create account"}</a></p></form></main>
}
